export enum RequestCalendarStatus {
    Idle = 'idle',
    Failed = 'failed',
    Error = 'error'
}

export enum AddTrainingStatus {
    Idle = 'idle',
    Success = 'success',
    Loading = 'loading',
    Error = 'error'
}

export enum RequestStatusType {
    Idle = 'idle',
    Succeeded = 'succeeded',
    Loading = 'loading',
    Failed = 'failed'
}
